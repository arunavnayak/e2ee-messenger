services:
  - type: web
    name: e2ee-messenger
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: e2ee-messenger-db
          property: connectionString
    autoDeploy: true

databases:
  - name: e2ee-messenger-db
    databaseName: e2ee_messenger
    user: e2ee_user
    plan: free
    region: oregon
